{
    "name": "usernamn0tavailable/crabmagick",
    "description": "Pure-Rust image processing for PHP — zero config. A CrabMagick daemon is spawned automatically on first use. No php.ini changes, no sudo, just composer require.",
    "type": "library",
    "license": "AGPL-3.0-or-later",
    "keywords": ["image", "rust", "vips", "webp", "jxl", "jpeg", "iiif", "daemon"],
    "require": {
        "php": ">=8.1"
    },
    "autoload": {
        "psr-4": {
            "CrabMagick\\": "src/"
        },
        "files": [
            "src/bootstrap.php",
            "src/functions.php"
        ]
    },
    "scripts": {
        "post-install-cmd": ["CrabMagick\\Installer::postInstall"],
        "post-update-cmd":  ["CrabMagick\\Installer::postInstall"]
    }
}
