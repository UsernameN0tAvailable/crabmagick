[package]
name = "oxipix-php"
version = "0.1.0"
edition = "2021"

[lib]
name = "oxipix"
crate-type = ["cdylib"]

[dependencies]
ext-php-rs = { version = "0.15.15", features = ["closure"] }
oxipix-core = { path = "../oxipix-core" }
